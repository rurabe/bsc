

<div class="row pagination-centered">
	<div class="span12">

		<div class="book-card paper">
			<div class="logo-head">
				<%= render :partial => 'shared/logo' %>
			</div>

      <div class="show-welcome">
        <%= render :partial => 'booklists/show_welcome' %>
      </div>

      <div class="deals">
        <%= render :partial => 'shared/deals' %>
      </div>

			<%= render :partial => 'booklists/book_card' %>
		</div>

	</div>
</div>

<%= render :partial => 'booklists/like_modal' %>

<% content_for :page_js do %>
	<%= javascript_include_tag "booklists/show", :debug => false %>
<% end %>